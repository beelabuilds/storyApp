# AI Integration Directory

This folder is reserved for the **Qwen3.5-9B** model integration.

## Future Implementation Details

- **Model**: Qwen3.5-9B (or suitable quantized model).
- **Service**: Will be integrated as a local inference or api service.
- **Hook**: The FastAPI backend in `backend/story_generator.py` will import the generation pipeline from this directory once configured.
